package io.pocat.context;

import org.junit.Test;

import java.io.IOException;

public class DefaultContextReaderTest {
    @Test
    public void testDefaultContextRead() throws IOException {
/*        DefaultContextReader reader = new DefaultContextReader();
        DefaultContextReader.DefaultContextElement contextElement = reader.readContext(DefaultContextReaderTest.class.getResourceAsStream("/context.xml"));
        System.out.println(contextElement);*/
    }
}
